import { Form } from './Form';
import styles from '../styles/app.module.css';

export const App = () => {
    return (
        <div className={styles.container}>
            <Form />
        </div>
    );
};
